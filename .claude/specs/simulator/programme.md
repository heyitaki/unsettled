# Programme and phase order

Class **D**: decisions, their rationale, and what each revision replaced. Not falsifiable by code. See [spec.md](spec.md) for the class rules.

The simulator exists to measure which starting placements win, and ultimately to calibrate the app's own scorer (`src/engine/weights.ts`). The phase order below was revised after two findings, and the revision matters more than the list: **weight tuning now runs last.**

This file covers phases A through J and the knight-timing item, all closed. The successor programme, which sharpens the placement scorer itself rather than the field it is measured against, is [placement-programme.md](placement-programme.md), phases SP0 through SP6.

Why: a full-power sweep of the `resourceValue` spread found the optimum is policy-dependent and tracks trading volume — a light-trading policy and a heavy-trading one disagree about the best value, and neither answer is the answer. Every result measured so far was measured against a field of *self-regarding* policies, which model opponents almost not at all. Making the field threat-aware will invalidate those results the same way trading threatened to. Tuning weights against a field that is about to be replaced spends held-out seed domains on answers that will not survive.

**Phases E, F and G were reassigned in this revision.** They previously meant "re-tune across the trading grid", "structural formula terms" and "adopt"; those survive as H and I below. An older note referring to Phase E or F means the old plan.

## Why E through G exist at all

Every policy the simulator started with maximizes its own progress and models opponents almost not at all; the Phase-D trade embargo was close to the only exception. The four targets E through G pursue — robber placement, dev-card timing, trade selection, goal switching — are all actions whose entire value lives in their effect on somebody else's trajectory. So this is a change of **objective**, not a feature list: from "maximize my VP rate" to "maximize my win probability". The distinction has teeth, because denial pays only through win probability. Taking Longest Road to strip two VP costs tempo, and if it does not change who wins, it is a straight loss.

There is a second payoff beyond sharpening the measuring instrument. `crates/engine` is deliberately wasm-portable so the app can eventually give mid-game recommendations rather than placement-only ones. A self-regarding policy cannot give mid-game advice worth reading: "take Longest Road now or settle?" is exactly the question that needs an opponent model.

## Phases

- **A — done.** The app formula as a playable arm, with a TS/Rust parity fixture.
- **B — done.** The paired `evaluate` harness.
- **C — retired.** Was the first tuning pass under no-trade. Superseded: tuning moved to the end, for the reason above.
- **D — built.** Player-to-player trading with the endgame embargo and farthest-from-winning counterparty selection.
- **E — built.** Opponent belief state. What it derives and guarantees is a contract; see [contracts.md](contracts.md).
- **F — built.** Expected turns to win, closed-form over the belief state. What it guarantees is a contract; see [contracts.md](contracts.md).
- **G — built and measured, singly and as a composite.** G1 robber placement, G2 belief-driven monopoly and dev-card timing, G3 threat-aware trading, and G4 threat-aware action selection and denial are built. G4 delivers contested-card pressure, bounded Longest Road and Largest Army defence, shared-target road racing, and denial-aware Road Building and goal inputs. As shipped it deliberately did not deliver plan persistence, route-cut blocking, ETW trade eligibility, or changes to the hardcoded building-action bands; the gap-closing batch after it added blocking of a rival's last approach to a contested vertex and moved trade eligibility onto the shared danger model. The couplings and gate boundaries are contracts; see [contracts.md](contracts.md).
- **H — done.** Re-tuned weights across the resulting grid, including the structural formula terms that were the old Phase F. This previously said to fix the scoring defects in [gaps.md](gaps.md) first; SIM-BATCH1 discharged the four named valuation defects. H0 (the params-file loader, `simulator/placement/sweep-bounds.json`, and the load-time building-band headroom check) discharged `SIM-GAP-30`: every swept vector is bounds-declared and headroom-checked before it can run. Sweep with every G gate on, for the reason M-21 gives. H3 settled the open `handValue` question below (M-43: drop recommended, ~0.1pp residual value recorded). **H1 (placement weights, M-38/M-39/M-40) and H2 (policy blocks, M-41/M-42) are screened.** H1 produced no winner at the threshold; its strongest prior is `diversityWeight` around 4.8, real but sub-threshold at 4x power. H2 produced nine `better` winners for the H4 combine, all on one surface: eight `TradeParams` axes plus `devBuyScale` 0.25, whose `better`-at-lower-scale verdict confirmed and closed `SIM-GAP-24`. Everything else measured `equivalent` or `inconclusive` inside the threshold, and much of the denial block is structurally dead at the screen's symmetric corner (zero discordant games between identical composites). One caveat on the M-41 dead list: `stageUrgencyWeight` was dead by construction rather than empirically — it acts only through `stageExpansionWeight`/`stageCityWeight`, both zero in every screen arm, so its zero-discordant reading was guaranteed before the run; screening it requires pairing it with a nonzero consumer weight. **`TradeConfig` is unscreenable by this instrument, structurally (D-class, M-41):** the config is engine-global, so no perturbation can leave the field unchanged, and the hero-only estimand does not exist; its defaults stay declared placeholders, and a per-seat refusal-threshold seam is the recorded follow-up if per-seat tuning is ever wanted. **H4 is done and H with it (M-44/M-45).** The interaction the screens could not see was large: the nine winners together were worth +10.2pp on tuning against an impossible ~+48pp sum of singles, and the coordinate pass at the combined point reverted five of them — `tempoWeight`, `tempoHalf`, and `benefitWeight` because removing each *improved* the combined vector (`better`), `marginScale` and `offerBase` because removal was value-neutral (`equivalent`; the +12.23pp `marginScale` single was fully absorbed by the other trade axes). The final vector keeps four winners — `trading.etwWeight` 4.0, `trading.dangerFloor` 4.0, `trading.dangerWeight` 2.0, `devBuyScale` 0.25 — measured +14.76pp on tuning (beating the full nine-winner vector by +4.56pp) and confirmed on the held-out `eval` domain at **+13.98pp `better`** (M-45, the single eval spend authorized for H; the ledger under [Seed-domain discipline](#seed-domain-discipline) records both spends). The candidate is committed as `simulator/placement/phase-i-candidate-params.json` (the confirmed policy vector) and `simulator/placement/phase-i-candidate-weights.json` (the pre-drop placement snapshot — H1 had no winner), pinned by test. Recorded Phase-I inputs alongside the candidate: the M-40 `diversityWeight` ~4.8 prior, the M-43 `handValue` drop recommendation, and the standing field-overfit caution — all four kept axes tune the hero's trade machinery against this exact fixed field, and pricing that residual on `gate` was the Phase-I question.
- **I — done, adopted (M-46).** The user delegated the decision on 2026-08-27; the preregistered gate run (the single authorized `gate` spend) measured the candidate **+14.10pp `better`** (clustered `[+13.65pp, +14.55pp]`), and per the preregistered rule the package shipped: the four policy axes are now the Rust defaults, and the M-43 `handValue` drop landed in `simulator/placement/default-weights.json` and `src/engine/weights.ts` (`handValueWeight` 0, term code kept). The `phase-i-candidate*` files stay as the adoption record, pinned by `the_phase_i_candidate_files_record_the_adopted_vectors`. The M-40 `diversityWeight` prior was not adopted. The field-overfit caution transfers to the adopted defaults: they are defaults for this composite policy against this field, and a field change re-opens tuning on fresh `tuning` seeds. One inherited edge condition: all four adopted values sit exactly on their committed `sweep-bounds.json` endpoints (M-42 recorded the three trade axes still rising at their bounds), and `handValueWeight` 0 sits on its declared minimum, so any future sweep of these axes must re-bracket the bounds around the adopted defaults before it can probe past them.
- **J — built and measured, singly and as a composite.** Build-target scoring landed as five zero-default terms: the shared goal-need model and its vertex term (J1), the stage signal (J2), the two piece-economy terms (J3), the goal-hysteresis margin (J4), and the frontier replacement for the degree-valued expansion count (J5), measured alone in M-32 through M-36 and as a composite in M-37. Every J weight ships at zero: four axes measured flat at their trial values, hysteresis measured decisively worse at both margins, and the composite response is near-additive, so H2 may sweep the J axes independently. The design decisions are recorded under [Phase J design decisions](#phase-j-design-decisions); the label-level guarantees are contracts; see [contracts.md](contracts.md).
- **Knight timing — built and measured.** The deferral G1 chose so its A/B stayed placement-only is discharged: the knight's contested-card and progress terms take the shared denial pressure, its steal term prices the victim through belief and the shared danger model, and play timing is priced at the robber pair the knight actually plays, joined to the placement value the chooser maximized. Measured `equivalent` in M-31 and kept per the gap-fix rule; `ThreatParams::knight_steal_weight` and `knight_placement_weight` are unswept Phase-H placeholders, and the frozen score survives behind the `-legacyknight` label.

## Phase J design decisions

Class-D record of the interview-settled Phase J design, as built.

**J ran before H, superseding the phase-list order.** The list above was written H-then-J. J was built and measured first because H tunes whatever scorer exists and the programme's own reordering principle is tuning-last: sweeping weights before J would have spent screens on a scorer J was about to change.

**One goal-need model.** The outstanding cost of the current goal minus hand and expected production is computed once (`policy::goal_need`) and shared by every consumer — the J1 vertex term and the J3 cost-pressure charge — on the same one-model rule the programme applies to opponent threat. The goal chooser structurally passes no need: a candidate goal's own need feeding the scores that select it would be a fixed point.

**Stage inputs are split by what a policy can see.** The primary stage input is remaining settlement/city pieces and distance-rule-open sites, pure public state available to every policy; the ETW-derived urgency input exists only under gated (`Option`-bearing) policies, because the setup and plain paths carry no belief state. Both are zero-default.

**Piece economy prices both halves of SIM-GAP-29.** A settlement-slot return on city value, scaling with proximity to the five-settlement cap and remaining legal sites, and a cost-pressure charge pricing a build's cost against the selected goal's need, reusing the goal-need machinery. An affordable goal has zero need by construction, so a build is never charged for its own goal.

**Hysteresis lives on per-game state, and lost.** The incumbent goal sits on `GameState` (reset in `GameArena::prepare`, per the extension-seams contract) and a challenger must beat it by a swept margin applied in the chooser's comparisons only. M-35 measured both trial margins decisively worse — re-optimizing the goal every decision beats commitment at this corner — so the margin ships at zero and a Phase-H sweep should treat the axis as likely zero-optimal.

**Frontier replaces degree inside the one expansion closure.** `policy::frontier::opened` blends with the degree count via a mix parameter inside `vertex_score`'s expansion closure, so build candidates, the goal chooser, and the road and pair expansion credits all price expansion through one expression; mix zero restores the degree count bit-for-bit.

**The increment rule held throughout.** Each term landed as its own commit with a paired A/B against the fixed full-composite reference, then one composite A/B (M-37, two arms: all five terms at lo trials, and the same with hysteresis held at its shipped zero) confirmed the response is near-additive — the composite penalty is the hysteresis penalty and nothing more, and no J interaction is detectable at the lo corner.

## Settled at H3: `handValue` does not survive ETW (M-43, drop recommended)

The question: the placement formula's `handValue` term prices the second settlement's immediate setup grant as cards in hand, but the belief state already tracks exact resource counts and ETW consumes them, so once play is ETW-aware the grant may already be counted there and the explicit term a double count. Two things kept it a real question rather than a formality: dropping the term reverts a *rules*-correctness fix at the formula level, so the null result and the correct result look alike unless the A/B is set up to tell them apart; and `handValue` prices cards through `resourceValue` with no scarcity scaling, defensible for a one-off setup draw and wrong for a general hand, so ETW subsuming it could be the better model rather than merely a redundant one.

The answer (M-43, preregistered): `handValueWeight: 0` against the ETW-aware composite field at 64,000 paired units came back `equivalent` at -0.13pp, clustered interval `[-0.25pp, -0.01pp]`, strictly inside the ±1pp threshold — the exact preregistered condition for recommending the drop. The discrimination came from the field: both arms play ETW-aware composites, so the null specifically reads "the placement-time term adds nothing ETW-aware play does not realize", and that is what was measured. The all-negative interval is recorded honestly: the term retains a real ~0.13pp of unique value on a small live surface (1,464 of 64,000 units discordant), an order of magnitude under the threshold. **Recommendation: drop the term (`handValueWeight: 0`) whenever placement is scored for ETW-aware play; the ~0.1pp residual says keeping it is also nearly free.** Adopted at Phase I (M-46): the shipped default is 0 in both scorers, the term code survives, and the SU-7 suite pins the mechanism at an explicit pre-drop witness weight.

## Throughput disposition — settled at H (D-class)

The G3 all-seat throughput floor mixed code efficiency with game length. `SIM-GAP-21` diagnosed the miss: the arm plays longer games that leave fuller boards, and the existing Longest Road search grows superlinearly in roads placed.

G4 therefore registered no all-seat games-per-second floor. M-20 observes a fixed number of policy decisions on the same state, gate off against gate on, and reports rather than gates the cost. It separately counts Longest Road network construction per decision so a hotter trail-search path is visible without confusing it with changed game length.

H closes the question it owned: **the per-decision fixed-state metric in M-22's form is the programme's registered efficiency measure, and there is no all-seat games-per-second floor of any kind.** A per-decision measurement isolates code cost from game length, which is the exact confusion `SIM-GAP-21` diagnosed; an all-seat affordability floor would re-import it, and the H screens themselves demonstrated affordability is not the binding constraint (176,000-game invocations complete in tens of seconds). A per-game throughput miss is a prompt to measure per-decision cost, never an efficiency verdict by itself.

## How to read the A/B results so far — settled by the composite arm

Each of the four built G consumers was measured alone against a field of today's policies, and every one came back `inconclusive` with a positive point estimate declining in merge order: M-01, M-02, M-03 and M-19 in [measurements.md](measurements.md). That was consistent with "each consumer is individually under-powered" and equally consistent with "this is not paying against this field", and nothing measured then separated them.

This section previously carried the resulting directive — run a composite arm before H, and re-baseline against a threat-aware field if it too came back inconclusive. That experiment has been run, so what follows records its outcome and the precondition is discharged.

**M-21 separated them, and the programme is paying.** With all four gates on against the field G3 was measured against, the composite arm's verdict is `better` at the preregistered threshold — the first non-`inconclusive` result the programme has produced — and the gates compound *super-additively*, three of the four being worth markedly more inside the composite than alone. So four inconclusive singles were a power result about how the consumers were measured, not a value result about whether they work. **H may proceed against this field**; re-baselining against a threat-aware field is no longer indicated, and neither held-out domain was spent settling it.

Three consequences bind what comes next.

**Measure the composite, not the singles, from here.** The design note below still holds for *attribution* — a bundle that wins teaches nothing about which part earned it — but a per-consumer A/B against a field with every other gate off now systematically understates a consumer, because the interaction is where most of the value is. Paired increment runs against a full-composite reference give both: attribution and the value at the corner that will actually ship.

**Denial was the exception, and its read has changed.** This paragraph previously recorded M-21's three `equivalent` reads as positive evidence that denial's effect was below the practical threshold, bounded by the defective goal chooser then named `SIM-GAP-02`. SIM-BATCH1 fixed that chooser and re-ran both M-21 fields. Denial is now `inconclusive` on each moved field: that does not establish value, but the former read-down is no longer justified. Treat the re-measurement in M-22 as the current result and carry denial into H rather than excluding it as settled.

**The interaction is trading.** On the plain field the three gates that field admits are close to additive; the super-additivity appears only once player trading exists. Whatever H sweeps, it must sweep with the trade gate on, or it will be tuning at a corner the value does not live in.

The rules-level `trade::embargoed` eligibility check runs on the shared ETW danger model (closing `SIM-GAP-27`, measured in M-27): a hard `TradeConfig::embargo_danger` threshold plus a softer `embargo_takeover_danger` that fires only under an imminent Largest Army or Longest Road swing, which the ETW closed form deliberately excludes. The legacy VP-estimate thresholds survive behind `LegacyValuation::vp_embargo` as the `-legacyembargo` reference arm. `ThreatParams`, `DevCardParams`, and `DenialParams` weights are unswept Phase-H placeholders; three `TradeParams` axes (`etwWeight`, `dangerFloor`, `dangerWeight`) are the swept, gate-adopted M-46 values and the rest of that struct stays placeholder. The `TradeConfig` defaults, including all three embargo fields, are likewise placeholders for a later parameter sweep, not tuned values.

## Seed-domain discipline

`--domain` deliberately has no default so tuning work cannot accidentally use a held-out domain. The domain constants and their disjointness assertion are contracts; see [contracts.md](contracts.md).

Spend the domains in order and never go back. Screen and tune on `tuning` freely; use `eval` once a candidate is settled, to check the tuning result was not an artifact of its seeds; keep `gate` for the final adoption decision only. A domain cannot be un-spent — once a parameter has been screened against a domain, that domain's estimate for *that parameter* is no longer unbiased, though it stays clean for every other parameter. `eval` has been spent twice: on the `resourceValue` spread question, and on the M-45 confirmation of the H4 final vector (the trade-axis and `devBuyScale` parameters). `gate` has been spent once, on the M-46 Phase-I adoption decision (user-delegated, preregistered). For the adopted vector all three domains are now spent: no unbiased re-measurement of it exists, and a genuinely new question needs new domains.

The already-collected `spread_*` and `gpf_*` arm results are **superseded and need re-running**, independently of the reordering above. `evaluate` scores arms against the field `simulator/placement/default-weights.json`, and the OBJ-8 fix added `handValue` to it, so every recorded arm-versus-field number was taken against a field that no longer exists. No option preserved comparability; the alternative is worse for the reason given under Weights files in [contracts.md](contracts.md). This is part of what already spent `eval` on the spread question.

Default heuristic parameters were tuned only on the named TUNING seed domain. The policy gates use the disjoint held-out EVAL seed domain and a fully crossed board × repetition × hero-seat schedule.

## How to read the gap inventory

[gaps.md](gaps.md) began as an audit of the policy layer after the four G consumers; SIM-BATCH1 closed the valuation defects it named, and the gap-closing batch after that closed every remaining entry it was scoped to, so what is left is setup-phase placement (`SIM-GAP-20`), `SIM-GAP-21`, kept as a diagnosis record rather than as work, and `SIM-GAP-33`, the contest block-bonus over-credit found in post-completion review, which waits for a preregistered `tuning` A/B because the Phase-I candidate was eval-confirmed with the current semantics (`SIM-GAP-24` closed with the M-41/M-42 `dev_buy_scale` sweep confirming its bias). The inventory stays grouped by what kind of work an entry is.

## Design notes that outlive their phase

**Measure each consumer separately.** A single policy bundling belief, ETW, robber, monopoly and goal-switching that wins by several points teaches nothing about which part earned it and leaves none of it tunable. The paired `evaluate` harness gives clean per-consumer A/B; use it.

**Pin the opponent model to a fixed reference implementation.** If opponents' ETW is computed with the same value function being tuned, the model moves with the arm and every sweep measures two changes at once.

**A lexicographic tie-break ladder is the wrong shape for ranking opponents.** Each rung fires only on an exact tie of the rung above, so replacing a coarse integer criterion with a continuous one makes ties vanish and silently deletes every lower rung. Use a scalar score with weighted terms and sweep the weights. SIM-BATCH1 applied this note to the main action scorer, replacing its city-over-settlement ladder with one building band ordered by marginal value. Relatedly, there is one opponent-threat function, not two: the acceptance rule's estimate of opponent gain and the counterparty-selection ranking are the same question over different arguments, and a parallel second model will drift out of agreement with the first.

**A card's discard cost is its conversion rate.** Holding a 2:1 wood port makes each wood worth about half a generic card against about a quarter for an unported resource, so the ported resource is the *last* thing to shed, not the first. Beware a naive `1 / rate` term, because the discrete marginal value oscillates: at a 2:1 rate, going from three wood to two loses only a dead spare, while going from two to one destroys a whole trade. Goal need still dominates conversion value when the two disagree, so this is a second term rather than a replacement. Built in `policy::exposure` (M-25) with one measured narrowing: the discrete bundle protection applies only at rates strictly better than the observer's base rate, because protecting bank-rate four-stacks sacrificed hand diversity for a marginal trade and dropped heuristic-v1 below the predeclared `policy_strength` gate against priority-trader.

**Build hand-size risk once as a shared exposure term** rather than patching each site, for the same reason the programme insists on one opponent-threat function: three independent spellings will drift. Built as `policy::exposure` (M-25): the discard's conversion ranking, the pre-emptive shedding trade, and the pre-roll seven charge on card-adding dev plays all read it; `LegacyValuation::exposure_blind` restores the exposure-blind pipeline as the `-legacyexposure` measurement reference.
